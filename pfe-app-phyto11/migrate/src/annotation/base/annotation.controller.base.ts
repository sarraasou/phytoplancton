/*
//------------------------------------------------------------------------------ 
// This code was generated by Amplication. 
// 
// Changes to this file will be lost if the code is regenerated. 
//
// There are other ways to to customize your code, see this doc to learn more
// https://docs.amplication.com/docs/how-to/custom-code
//
//------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";import { MockUserRoles } from "src/mockUserRole";import { AllowAllGuard } from "src/allowGuard";

import * as swagger from "@nestjs/swagger";
import { BatchPayload } from "../../BatchPayload";
import { Logger } from "winston";
import { fileDto } from "../../file.dto";
import * as XLSX from "xlsx";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import * as abacUtil from "../../auth/abac.util";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { PaginatedInterface } from "../../util/PaginatedInterface";
import { flattenObject } from "src/util/helpers";
import { AnnotationService } from "../annotation.service";
import { AnnotationCreateInput } from "./AnnotationCreateInput";
import { AnnotationCreateManyInput } from "./AnnotationCreateManyInput";
import { AnnotationWhereInput } from "./AnnotationWhereInput";
import { AnnotationWhereUniqueInput } from "./AnnotationWhereUniqueInput";
import { AnnotationFindManyArgs } from "./AnnotationFindManyArgs";
import { AnnotationupdateManyArgs } from "./AnnotationupdateManyArgs";
import { AnnotationUpdateInput } from "./AnnotationUpdateInput";
import { Annotation } from "./Annotation";
import { getListAnnotationDto } from "./getListAnnotation.dto";
@swagger.ApiBearerAuth()
export class AnnotationControllerBase {
  constructor(
    protected readonly service: AnnotationService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder,
    protected readonly logger: Logger
  ) {}

  @common.UseGuards(
    AllowAllGuard
  )
  @common.Post()
  @nestAccessControl.UseRoles({
    resource: "Annotation",
    action: "create",
    possession: "any",
  })
  @swagger.ApiCreatedResponse({ type: Annotation })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(
    @common.Body() data: AnnotationCreateInput,
    @MockUserRoles() userRoles: string[]
  ): Promise<Annotation> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "create",
      possession: "any",
      resource: "Annotation",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      this.logger.log({
        level: "error",
        message: `providing the properties: ${properties} on ${"Annotation"} creation is forbidden for roles: ${roles}`,
      });

      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"Annotation"} creation is forbidden for roles: ${roles}`
      );
    }
    return await this.service.create({
      data: {
        ...data,

        aimodel: {
          connect: data.aimodel,
        },

        image: {
          connect: data.image,
        },

        validatedBy: data.validatedBy
          ? {
              connect: data.validatedBy,
            }
          : undefined,
      },
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        deletedAt: true,

        aimodel: {
          select: {
            id: true,
          },
        },

        image: {
          select: {
            id: true,
          },
        },

        validatedBy: {
          select: {
            id: true,
          },
        },
      },
    });
  }

  // @common.UseGuards(
  //   defaultAuthGuard.DefaultAuthGuard,
  //   nestAccessControl.ACGuard
  // )
  // @common.Post("/createMany")
  // @nestAccessControl.UseRoles({
  //   resource: "Annotation",
  //   action: "create",
  //   possession: "any",
  // })
  // @swagger.ApiCreatedResponse({ type: Annotation })
  // @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  // async createMany(
  //   @common.Body() data: Array<AnnotationCreateManyInput>,
  //   @MockUserRoles() userRoles: string[]
  // ): Promise<BatchPayload | null> {
  //   const permission = this.rolesBuilder.permission({
  //     role: userRoles,
  //     action: "create",
  //     possession: "any",
  //     resource: "Annotation",
  //   });
  //   const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
  //   if (invalidAttributes.length) {
  //     const properties = invalidAttributes
  //       .map((attribute: string) => JSON.stringify(attribute))
  //       .join(", ");
  //     const roles = userRoles
  //       .map((role: string) => JSON.stringify(role))
  //       .join(",");
  //     this.logger.log({
  //       level: "error",
  //       message: `providing the properties: ${properties} on ${"Annotation"} creation is forbidden for roles: ${roles}`,
  //     });

  //     throw new errors.ForbiddenException(
  //       `providing the properties: ${properties} on ${"Annotation"} creation is forbidden for roles: ${roles}`
  //     );
  //   }
  //   return await this.service.createMany({
  //     data: data,
  //     skipDuplicates: false,
  //   });
  // }

  @common.UseGuards(
    AllowAllGuard
  )
  @common.Get()
  @nestAccessControl.UseRoles({
    resource: "Annotation",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: getListAnnotationDto })
  @swagger.ApiForbiddenResponse()
  @swagger.ApiQuery({
    type: () => AnnotationFindManyArgs,
    style: "deepObject",
    explode: true,
  })
  async findMany(
    @common.Req() request: Request,
    @MockUserRoles() userRoles: string[]
  ): Promise<PaginatedInterface<Annotation>> {
    const args = plainToClass(AnnotationFindManyArgs, request.query);

    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Annotation",
    });
    const results = await this.service.findMany({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        deletedAt: true,

        aimodel: {
          select: {
            id: true,
          },
        },

        image: {
          select: {
            id: true,
          },
        },

        validatedBy: {
          select: {
            id: true,
          },
        },
      },
    });
    const result = results.paginatedResult.map((result: Annotation) =>
      permission.filter(result)
    );
    return { paginatedResult: result, totalCount: results.totalCount };
  }

  @common.UseGuards(
    AllowAllGuard
  )
  @common.Get("/fileExcel")
  @nestAccessControl.UseRoles({
    resource: "Annotation",
    action: "read",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: fileDto })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findDataForExcel(
    @MockUserRoles() userRoles: string[]
  ): Promise<fileDto> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "any",
      resource: "Annotation",
    });
    const results = await this.service.findMany({
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        deletedAt: true,

        aimodel: {
          select: {
            id: true,
          },
        },

        image: {
          select: {
            id: true,
          },
        },

        validatedBy: {
          select: {
            id: true,
          },
        },
      },
    });
    const result = results.paginatedResult.map((result: Annotation) =>
      permission.filter(result)
    );
    const flatResult = result.map((obj) => flattenObject(obj));
    var excelFile = XLSX.utils.json_to_sheet(flatResult);

    var Workbook = XLSX.utils.book_new();
    await XLSX.utils.book_append_sheet(Workbook, excelFile, "data");
    const file = await XLSX.write(Workbook, {
      bookType: "xlsx",
      bookSST: true,
      type: "base64",
    });
    return { file: file };
  }

  @common.UseGuards(
    AllowAllGuard
  )
  @common.Get("/:id")
  @nestAccessControl.UseRoles({
    resource: "Annotation",
    action: "read",
    possession: "own",
  })
  @swagger.ApiOkResponse({ type: Annotation })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: AnnotationWhereUniqueInput,
    @MockUserRoles() userRoles: string[]
  ): Promise<Annotation | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "read",
      possession: "own",
      resource: "Annotation",
    });
    const result = await this.service.findOne({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        deletedAt: true,

        aimodel: {
          select: {
            id: true,
          },
        },

        image: {
          select: {
            id: true,
          },
        },

        validatedBy: {
          select: {
            id: true,
          },
        },
      },
    });
    if (result === null) {
      this.logger.log({
        level: "error",
        message: `No resource was found for ${JSON.stringify(params)}`,
      });
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return permission.filter(result);
  }

  @common.UseGuards(
    AllowAllGuard
  )
  @common.Patch("/:id")
  @nestAccessControl.UseRoles({
    resource: "Annotation",
    action: "update",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: Annotation })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: AnnotationWhereUniqueInput,
    @common.Body()
    data: AnnotationUpdateInput,
    @MockUserRoles() userRoles: string[]
  ): Promise<Annotation | null> {
    const permission = this.rolesBuilder.permission({
      role: userRoles,
      action: "update",
      possession: "any",
      resource: "Annotation",
    });
    const invalidAttributes = abacUtil.getInvalidAttributes(permission, data);
    if (invalidAttributes.length) {
      const properties = invalidAttributes
        .map((attribute: string) => JSON.stringify(attribute))
        .join(", ");
      const roles = userRoles
        .map((role: string) => JSON.stringify(role))
        .join(",");
      this.logger.log({
        level: "error",
        message: `No resource was found for ${JSON.stringify(params)}`,
      });
      throw new errors.ForbiddenException(
        `providing the properties: ${properties} on ${"Annotation"} update is forbidden for roles: ${roles}`
      );
    }
    try {
      return await this.service.update({
        where: params,
        data: {
          ...data,

          aimodel: {
            connect: data.aimodel,
          },

          image: {
            connect: data.image,
          },

          validatedBy: data.validatedBy
            ? {
                connect: data.validatedBy,
              }
            : undefined,
        },
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          deletedAt: true,

          aimodel: {
            select: {
              id: true,
            },
          },

          image: {
            select: {
              id: true,
            },
          },

          validatedBy: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error: any) {
      if (isRecordNotFoundError(error)) {
        this.logger.log({
          level: "error",
          message: `No resource was found for ${JSON.stringify(params)}`,
        });
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.UseGuards(
    AllowAllGuard
  )
  @common.Delete("/:id")
  @nestAccessControl.UseRoles({
    resource: "Annotation",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiOkResponse({ type: Annotation })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: AnnotationWhereUniqueInput
  ): Promise<Annotation | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          deletedAt: true,

          aimodel: {
            select: {
              id: true,
            },
          },

          image: {
            select: {
              id: true,
            },
          },

          validatedBy: {
            select: {
              id: true,
            },
          },
        },
      });
    } catch (error: any) {
      if (isRecordNotFoundError(error)) {
        this.logger.log({
          level: "error",
          message: `No resource was found for ${JSON.stringify(params)}`,
        });
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
